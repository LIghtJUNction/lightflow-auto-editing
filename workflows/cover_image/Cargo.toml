[package]
name = "lightflow-video-cover-image"
version = "0.1.0"
edition = "2024"
description = "LightFlow workflow for extracting and composing video cover images."
license = "MIT OR Apache-2.0"
repository = "https://github.com/lightjunction/lightflow-auto-editing"
keywords = ["lightflow", "workflow", "video", "cover"]
categories = ["multimedia::video", "development-tools"]

[package.metadata.lightflow]
runner = "lightflow-video-cover-image-runner"

[[bin]]
name = "lightflow-video-cover-image-runner"
path = "src/bin/runner.rs"

[dependencies]
lightflow = { workspace = true }
lightflow-auto-edit-runtime = { workspace = true }
